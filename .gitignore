node_modules

# Секреты (ключи Supabase и т.д.)
.env
.env.local
.env.*.local

# Автогенерация из .env.local (секреты)
public/cloud-config.json

# Cloudflare Worker локальные файлы
cloudflare-worker/.wrangler/
cloudflare-worker/.worker-url
cloudflare-worker/node_modules/

# Party server URL после деплоя
server/.party-url

# Сборка Vite (восстанавливается `npm run build`; дубликат public + бандла)
dist/

# Логи IDE / отладки
.cursor/*.log

# Кэш TypeScript
*.tsbuildinfo

# Unity внутри web-репо: Tailwind по умолчанию сканирует всё кроме node_modules/.gitignore —
# PackageCache и .tmp-* ломают Vite (ENOENT). Держи Unity снаружи или игнорируй целиком.
Setup Guide In-Editor Tutorial/
My project/
